<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lims.manage.erp.mapper.TestProductItemStandardFileRelDao">

    <resultMap type="com.lims.manage.erp.entity.TestProductItemStandardFileRel" id="TestProductItemStandardFileRelMap">
        <result property="checkItemId" column="check_item_id" jdbcType="INTEGER"/>
        <result property="standardFileId" column="standard_file_id" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="checkItemId" useGeneratedKeys="true">
        insert into test_product_item_standard_file_rel()
        values
        <foreach collection="entities" item="entity" separator=",">
        ()
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="checkItemId" useGeneratedKeys="true">
        insert into test_product_item_standard_file_rel()
        values
        <foreach collection="entities" item="entity" separator=",">
            ()
        </foreach>
        on duplicate key update
             </insert>

</mapper>

