<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lims.manage.erp.mapper.UserPlanInfoDao">

    <!--  获取计划报名的用户信息  -->
    <select id="getPlanUserInfo" resultType="com.lims.manage.erp.entity.SysUserEntity">
        SELECT userInfo.`name`,userInfo.username
        FROM t_user_plan_info planInfo
        LEFT JOIN sys_user userInfo ON planInfo.user_id = userInfo.user_id
        WHERE planInfo.plan_id=#{planId}
    </select>

</mapper>